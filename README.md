# SensortagSAPCloud
An implementation of a SAP Cloud Connection in the SensorTag App provided by Texas Instruments for connection with external IoT devices. Currently HTTP, WebSocket, and MQTT connections are implemeted. Work on MQTT via TCP is in process.
